#!/usr/bin/env python
'''from lib import set_parser_ver02, classes
import sys

sentences = set_parser_ver02.parse(sys.stdin)

for sentence in sentences:
    print "\n" + sentence.getSentence()
    for clause in sentence.clauses:
        for phrase in clause.phrases:
            print phrase.getPhrase()'''